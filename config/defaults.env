# strand-creator defaults (override via environment or CLI flags)

# Shared assets on the cluster (recommended)
ASSETS_DIR=${ASSETS_DIR:-/data/cephfs-1/work/groups/sanders/strand-creator-assets}

# Reference (required)
REF_FASTA=${REF_FASTA:-$ASSETS_DIR/refs/GRCh38_full_analysis_set_plus_decoy_hla.fa}

# Optional defaults
CHROMS_FILE=${CHROMS_FILE:-$ROOT/config/chroms.grch38.txt}
COVERAGE=${COVERAGE:-0.04}
NOISE=${NOISE:-5.0}
THREADS=${THREADS:-16}
MIN_SNPS_FOR_PHASING=${MIN_SNPS_FOR_PHASING:-5}

# SNP sample for haplotype SNV truth and optional HP tagging
SNP_SAMPLE=${SNP_SAMPLE:-HG00732}
